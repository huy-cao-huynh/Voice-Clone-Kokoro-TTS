# Voice-Clone-Kokoro-TTS
A parameter efficient TTS model capable of zero-shot voice cloning with multi-lingual support.

Training data format, `lang_code` fields, and Kokoro `repo_id` / vocabulary rules are documented in [voice_clone/DATASET.md](voice_clone/DATASET.md).
