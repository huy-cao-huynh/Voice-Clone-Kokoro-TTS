# Voice-clone training + inference dependencies (Kokoro fork + WavLM + HF).
#
# Install PyTorch FIRST for your hardware (ROCm / CUDA / CPU). This file does not
# pin torch, so a plain `pip install -r` would pull the default wheel from PyPI,
# which is usually wrong for AMD GPUs. See docs/environment.md.
#
# From the repository root, after torch+torchaudio are installed:
#   pip install -r requirements-training.txt
#
# Audio loading in training uses soundfile (see voice_clone/dataset.py) so ROCm/CPU stacks
# work without TorchCodec, which pulls CUDA NVRTC and breaks on non-NVIDIA PyTorch builds.
# System package: libsndfile (e.g. apt install libsndfile1, or conda libsndfile).

-e ./kokoro

soundfile>=0.12.0
transformers>=4.36.0
huggingface_hub>=0.20.0
wandb>=0.16.0
